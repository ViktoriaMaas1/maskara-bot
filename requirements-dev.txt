# ============================================================
# Dev dependencies — только для разработки/тестирования.
# В production образ Docker НЕ ставит эти пакеты.
#
# Установка:  pip install -r requirements-dev.txt
# (автоматически тянет requirements.txt через -r)
# ============================================================

-r requirements.txt

# --- Тесты ---
pytest==8.3.4
pytest-asyncio==0.24.0      # async тесты
pytest-cov==6.0.0           # coverage отчёты

# --- Линтеры / форматирование ---
ruff==0.8.4                 # быстрый линтер + форматтер (заменяет flake8 + black)
mypy==1.13.0                # статические типы

# --- Удобство разработки ---
ipython==8.30.0             # интерактивный shell
